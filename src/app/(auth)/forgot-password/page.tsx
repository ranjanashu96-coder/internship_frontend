"use client";import {useState} from 'react';import {toast} from 'sonner';import {authService} from '@/lib/services';import {Button,Input} from '@/components/ui';export default function Page(){const [email,setEmail]=useState('');return <div className="grid min-h-screen place-items-center p-6"><div className="card w-full max-w-md"><h1 className="text-2xl font-bold">Forgot password</h1><p className="my-4 text-sm text-slate-500">We will send a reset link to your registered email.</p><Input type="email" value={email} onChange={e=>setEmail(e.target.value)}/><Button className="mt-4 w-full" onClick={async()=>{await authService.forgot(email);toast.success('Reset link sent')}}>Send reset link</Button></div></div>}