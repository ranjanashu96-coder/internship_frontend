"use client";import {useSearchParams,useRouter} from 'next/navigation';import {useState} from 'react';import {toast} from 'sonner';import {authService} from '@/lib/services';import {Button,Input} from '@/components/ui';export default function Page(){const token=useSearchParams().get('token')??'';const [password,setPassword]=useState('');const r=useRouter();return <div className="grid min-h-screen place-items-center p-6"><div className="card w-full max-w-md"><h1 className="text-2xl font-bold">Reset password</h1><Input className="mt-4" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password"/><Button className="mt-4 w-full" onClick={async()=>{await authService.reset(token,password);toast.success('Password updated');r.push('/login')}}>Update password</Button></div></div>}