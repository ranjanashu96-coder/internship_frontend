"use client";import {flexRender,getCoreRowModel,getFilteredRowModel,getSortedRowModel,useReactTable,type ColumnDef} from '@tanstack/react-table';import {useState} from 'react';import {Input} from './ui';export function DataTable<T>({data,columns,searchPlaceholder='Search...'}:{data:T[];columns:ColumnDef<T>[];searchPlaceholder?:string}){const [filter,setFilter]=useState('');const t=useReactTable({data,columns,state:{globalFilter:filter},onGlobalFilterChange:setFilter,getCoreRowModel:getCoreRowModel(),getFilteredRowModel:getFilteredRowModel(),getSortedRowModel:getSortedRowModel()});return <div className="card overflow-hidden p-0"><div className="border-b p-4"><Input className="max-w-sm" value={filter} onChange={e=>setFilter(e.target.value)} placeholder={searchPlaceholder}/></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50">{t.getHeaderGroups().map(h=><tr key={h.id}>{h.headers.map(x=><th key={x.id} onClick={x.column.getToggleSortingHandler()} className="whitespace-nowrap px-4 py-3 text-left font-semibold">{flexRender(x.column.columnDef.header,x.getContext())}</th>)}</tr>)}</thead><tbody>{t.getRowModel().rows.map(r=><tr key={r.id} className="border-t hover:bg-slate-50">{r.getVisibleCells().map(c=><td key={c.id} className="whitespace-nowrap px-4 py-3">{flexRender(c.column.columnDef.cell,c.getContext())}</td>)}</tr>)}</tbody></table></div></div>}