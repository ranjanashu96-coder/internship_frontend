import type {LucideIcon} from 'lucide-react';export function StatCard({label,value,icon:Icon,helper}:{label:string;value:string|number;icon:LucideIcon;helper?:string}){return <div className="card"><div className="flex items-start justify-between"><div><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p>{helper&&<p className="mt-2 text-xs text-emerald-600">{helper}</p>}</div><div className="rounded-2xl bg-blue-50 p-3 text-blue-600"><Icon size={22}/></div></div></div>}